## Install Java 21 on Linux

```bash
sudo apt update
sudo apt install openjdk-21-jre-headless
```



## Add `bin` directory of Kafka on a shell session temporarily 

```bash
cd tools/kafka/

export PATH=$PATH:$(pwd)/bin/
```

